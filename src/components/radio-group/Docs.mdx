import { Meta, Markdown, Stories } from "@storybook/blocks";
import * as RadioGroupStories from "./RadioGroup.stories";

<Meta title="components/RadioGroup" of={RadioGroupStories} />

# RadioGroup

A combination of generic [useOptionFieldProps](?path=/docs/hooks-useoptionfieldprops--docs) and [useOptions](?path=/docs/hooks-useoptions--docs)
which renders the options as a list of `<input type="radio" />`.

```ts
import { RadioGroup } from "@form-atoms/field";
```

<Stories />
