import {
  Meta,
  Source,
  Canvas,
  Controls,
  Markdown,
  Description,
} from "@storybook/blocks";
import * as RequirementIndicatorStories from "./RequirementIndicator.stories";
import Props from "./props.md?raw";

<Meta title="components/RequirementIndicator" />

# RequirementIndicator

<Description of={RequirementIndicatorStories} />
<Canvas sourceState="none" of={RequirementIndicatorStories.Intro} />

```tsx
import { RequirementIndicator, textField } from "@form-atoms/field";

<RequirementIndicator field={numberField()} kind="icon" />
<RequirementIndicator field={numberField()} kind="label" />
<RequirementIndicator field={numberField({ optional: true })} kind="icon" />
<RequirementIndicator field={numberField({ optional: true })} kind="label" />
```

## Features

- ✅ Hooks into field's required state

## Usage

```ts
import { RequirementIndicator } from "@form-atoms/field";
```

<Markdown>{Props}</Markdown>
