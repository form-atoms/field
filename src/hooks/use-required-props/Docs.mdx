import { Meta, Markdown, Canvas } from "@storybook/blocks";

import Combinations from "./combinations.md?raw";

<Meta title="hooks/useRequiredProps" />

<hgroup>

<p class="category">Hooks</p>

# `useRequiredProps({field, required})`

A 'mixin' hook to compute `required` prop for the input elements based on the field's optionality.

</hgroup>

The required prop causes the browser to display the default 'requirement popups' over the inputs. To turn off this behavior, the hook
accepts manual `required` prop. Together with field's optionality, this gives four possible combinations:

<Markdown>{Combinations}</Markdown>
